# Room schema exports

Room (via KSP) writes JSON schema snapshots here using the `room.schemaLocation` arg configured in `core-data/build.gradle.kts`. Commit schema changes alongside database version bumps/migrations to track history.
